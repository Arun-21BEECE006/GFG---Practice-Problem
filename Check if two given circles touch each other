class Solution {
    int circleTouch(int X1, int Y1, int R1, int X2, int Y2, int R2) {
        int sum = (int) Math.sqrt((X2-X1)*(X2-X1) + (Y2-Y1)*(Y2-Y1));
        int r = R1+R2;
        if(sum <= r){
            return 1;
        }
        else{
            return 0;
        }
    }
}
